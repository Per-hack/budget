import React from 'react';

const HelpPage = () => (  // help page is a component since it returns JSX
    <div>
        This is from my help component
    </div>
    );
    
    export default HelpPage;